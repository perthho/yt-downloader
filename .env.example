# Example environment variables for local development and deployment
# Copy this file to `.env` and edit values for local testing (don't commit secrets)

# Where downloads are stored on the server/container
UPLOAD_FOLDER=/app/downloads

# Maximum allowed file size in bytes (default ~5GB)
MAX_CONTENT_LENGTH=5242880000

# The port your WSGI server should bind to (Heroku uses $PORT)
PORT=5000

# Flask environment (development/production)
FLASK_ENV=production
